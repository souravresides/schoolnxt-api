namespace SchoolNexAPI.Services.Abstract.Fee
{
    public interface IInvoiceNumberProvider
    {
        //Task<long> GetNextAsync(CancellationToken ct = default);
        // Optionally provide formatted string helper
        //Task<string> GetNextFormattedAsync(string prefix = null, CancellationToken ct = default);
    }
}
